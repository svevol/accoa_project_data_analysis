from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Finding how to increase accoa availability in p. putida',
    author='martamatos',
    license='MIT',
)
